@{
    ViewData["Title"] = "LandingP Page";
}

@* <div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
 *@

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CoolTech Fridges</title>
    <style>
        /* ====== RESET ====== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            color: #333;
            background-color: #f9f9f9;
            line-height: 1.6;
        }

        /* ====== HEADER ====== */
        header {
            background: #0d47a1;
            color: white;
            padding: 15px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

            header h1 {
                font-size: 1.5rem;
            }

        nav {
            display: flex;
            gap: 20px;
        }

            nav a {
                color: white;
                text-decoration: none;
                font-weight: bold;
                transition: color 0.3s;
            }

                nav a:hover {
                    color: #ffca28;
                }

        /* ====== HERO ====== */
        .hero {
            background: url('../images/Fridgesystem.jpg') center/cover no-repeat;
            height: 90vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            position: relative;
        }

            .hero::after {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0,0,0,0.5);
            }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 700px;
        }

        .hero h2 {
            font-size: 3rem;
            margin-bottom: 20px;
        }

        .hero button {
            padding: 15px 30px;
            font-size: 1rem;
            border: none;
            background: #ffca28;
            color: #333;
            cursor: pointer;
            border-radius: 5px;
            transition: background 0.3s;
        }

            .hero button:hover {
                background: #ffc107;
            }

        /* ====== FEATURES ====== */
        .features {
            display: flex;
            justify-content: space-around;
            padding: 60px 20px;
            background: white;
            flex-wrap: wrap;
            gap: 30px;
        }

        .feature {
            max-width: 300px;
            text-align: center;
        }

            .feature img {
                width: 70px;
                margin-bottom: 15px;
            }

        /* ====== CTA ====== */
        .cta {
            background: #0d47a1;
            color: white;
            text-align: center;
            padding: 60px 20px;
        }

            .cta button {
                padding: 15px 40px;
                font-size: 1.2rem;
                border: none;
                background: #ffca28;
                color: #333;
                cursor: pointer;
                border-radius: 5px;
                margin-top: 20px;
                transition: background 0.3s;
            }

                .cta button:hover {
                    background: #ffc107;
                }

        /* ====== FOOTER ====== */
        footer {
            background: #222;
            color: #aaa;
            text-align: center;
            padding: 20px;
            font-size: 0.9rem;
        }

        /* ====== MOBILE MENU ====== */
        .menu-toggle {
            display: none;
            flex-direction: column;
            cursor: pointer;
        }

            .menu-toggle span {
                background: white;
                height: 3px;
                width: 25px;
                margin: 4px 0;
            }

        @@media (max-width: 768px) {
            nav {
                display: none;
                flex-direction: column;
                background: #0d47a1;
                position: absolute;
                top: 60px;
                right: 0;
                width: 200px;
                padding: 20px;
            }

                nav.active {
                    display: flex;
                }

            .menu-toggle {
                display: flex;
            }
        }
    </style>
</head>
<body>

    <!-- ====== HEADER ====== -->
    @*     <header>
        <h1>CoolTech Fridges</h1>
        <div class="menu-toggle" id="menu-toggle">
            <span></span>
            <span></span>
            <span></span>
        </div>
        <nav id="nav">
            <a href="#contact">Contact</a>
            <a href="Login.html">Login</a>
        </nav>
    </header> *@

    <!-- ====== HERO ====== -->
    <section class="hero">
        <div class="hero-content">
            <h2>Keep It Fresh. Keep It Cool.With CoolTech</h2>
            <p>Smart, energy-efficient fridges designed for our customer satisfaction.</p>
            <button onclick="scrollToSection('shop')">Shop Now</button>
        </div>
    </section>

    <!-- ====== FEATURES ====== -->
    <section id="features" class="features">

        <div class="feature">
            <img src="https://img.icons8.com/ios-filled/100/0d47a1/wifi.png" alt="Smart Features">
            <h3>Smart Features</h3>
            <p>Wi-Fi enabled controls and temperature monitoring at your fingertips.</p>
        </div>
        <div class="feature">
            <img src="https://img.icons8.com/ios-filled/100/0d47a1/design.png" alt="Modern Design">
            <h3>Modern Design</h3>
            <p>Sleek, stylish, and built to complement any kitchen.</p>
        </div>
    </section>

    <!-- ====== CTA ====== -->
    <section class="cta" id="shop">
        <h2>Ready to Upgrade Your Fridge? Say Less We've got YOUR Back'</h2>
        <p>Discover our full range of premium fridges today.</p>
        <button>Browse Collection</button>
    </section>





</body>
</html>